export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

// math 모듈의 기본값
export default function multiply(a, b) {
  return a * b;
}

// module.exports = {
//   add,
//   sub,
// };
